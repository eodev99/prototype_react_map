import React from 'react'
import { MapContainer, GeoJSON } from "react-leaflet";

const DiseaseMap = ({ areas }) => {
    return (
        <div>
            
        </div>
    )
}

export default DiseaseMap
